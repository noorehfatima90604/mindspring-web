# Copy to .env.local — Vite reads VITE_* at build time.

# Backend API (required on real devices; use your deployed HTTPS URL)
# VITE_API_BASE=https://your-api.example.com

# Optional — Supabase auth
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
